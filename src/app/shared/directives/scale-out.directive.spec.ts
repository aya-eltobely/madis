import { ScaleOutDirective } from './scale-out.directive';

describe('ScaleOutDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleOutDirective();
    expect(directive).toBeTruthy();
  });
});
